import {Route} from '@angular/router';
import {SubscriptionComponent} from '@feature/subscription/pages/subscription';

export const subscriptionRoutes: Route[] = [
  { path: '', component: SubscriptionComponent },
]
